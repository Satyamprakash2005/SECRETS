This is a Node.js web application that lets users register, log in, and submit anonymous secrets.
It supports local authentication (with email and password) as well as Google OAuth2 login using Passport.js.
